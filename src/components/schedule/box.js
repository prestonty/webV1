// import function that displaysBox and add onClick after

export default function box() {
  return (
    <button>
      <p>{props.courseCode}</p>
      <p>{props.beginTime}</p>
    </button>
  );
}

// have an onClick function
// when you click, display full name, and location
